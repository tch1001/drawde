/** drawde's own interaction mode ids, registered on the EmbedPDF interaction manager. */
export const BOX_MODE = 'drawdeBox';
/** EmbedPDF's built-in text-selection mode. */
export const TEXT_MODE = 'pointerMode';
/** EmbedPDF's pan/hand mode — the default on touch devices. */
export const PAN_MODE = 'panMode';
